{
    "version": 2,
    "builds": [
        {
            "src": "streamlit app app.py --server.port 5000",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}

